# 🚀 LightRAG — 项目完整信息文档

> **生成时间**: 2026-06-29  
> **代码索引**: 8651 节点 · 42486 边 · 489+ 类  
> **版权**: HKUDS (香港大学数据科学实验室) · MIT License  
> **作者**: Zirui Guo  
> **仓库**: https://github.com/HKUDS/LightRAG

---

## 📋 目录

1. [项目概述](#1-项目概述)
2. [顶层目录结构](#2-顶层目录结构)
3. [核心架构](#3-核心架构)
4. [存储层](#4-存储层)
5. [文档摄入管道](#5-文档摄入管道)
6. [解析器层](#6-解析器层)
   - 6.4 [文件存储位置详解](#64-文件存储位置详解)
7. [分块策略](#7-分块策略)
8. [LLM 提供者层](#8-llm-提供者层)
9. [查询系统](#9-查询系统)
10. [角色化 LLM 配置](#10-角色化-llm-配置)
11. [API 服务器](#11-api-服务器)
12. [前端 WebUI](#12-前端-webui)
13. [配置与部署](#13-配置与部署)
14. [开发命令](#14-开发命令)
15. [测试体系](#15-测试体系)
16. [关键设计模式](#16-关键设计模式)

---

## 1. 项目概述

**LightRAG** 是一个基于图知识表示的检索增强生成（RAG）框架。它从文档中提取实体和关系，构建知识图谱，并通过多种检索模式（`local`、`global`、`hybrid`、`mix`、`naive`）来回答查询。与传统的纯向量检索 RAG 不同，LightRAG 利用图结构来捕捉实体间的语义关联，实现更深层次的上下文理解。

### 核心特性

| 特性 | 说明 |
|---|---|
| 🧠 **图驱动 RAG** | 自动从文本提取实体和关系，构建知识图谱 |
| 🔍 **5 种检索模式** | local / global / hybrid / mix / naive，适应不同场景 |
| 📄 **多模态文档** | 支持 PDF、DOCX、PPTX、XLSX、Markdown、图片等格式 |
| 🔌 **14+ LLM 后端** | OpenAI / Anthropic / Gemini / Ollama / Bedrock / Zhipu / VoyageAI / Jina 等 |
| 💾 **12+ 存储后端** | NetworkX / Neo4j / PostgreSQL / MongoDB / Redis / Milvus / Qdrant / FAISS / OpenSearch / Memgraph / JSON / NanoVectorDB |
| 🌐 **完整 WebUI** | React 19 + Sigma.js 图谱可视化 + 文档管理 + 聊天界面 |
| 🎯 **角色 LLM** | 4 个独立角色（extract / keyword / query / vlm），各自独立配置 |
| ✂️ **4 种分块策略** | 固定 Token / 递归字符 / 语义向量 / 段落语义 |
| 🐳 **容器化部署** | 多架构 Docker 镜像 + Kubernetes 部署清单 |
| 📊 **可观测性** | Langfuse 追踪 + RAGAS 评估集成 |

### 技术栈

```
后端: Python ≥3.10 · FastAPI · asyncio · NetworkX · NumPy · Pydantic
前端: React 19 · TypeScript · Vite · Tailwind CSS 4 · Sigma.js · Zustand
包管理: uv (Python) · Bun (前端)
数据库: 12+ 可插拔后端 (图/向量/键值/文档状态)
```

---

## 2. 顶层目录结构

```
LightRAG/
├── lightrag/                    # 🔴 核心 Python 包 (主代码)
│   ├── __init__.py              #   公开 API (懒加载)
│   ├── lightrag.py              #   LightRAG 主类 (~5000 行)
│   ├── operate.py               #   核心操作: 提取/合并/查询 (~5000 行)
│   ├── base.py                  #   存储基类 + 数据模型 (~1050 行)
│   ├── pipeline.py              #   文档摄入管道 (~4500 行)
│   ├── llm_roles.py             #   角色化 LLM 配置
│   ├── storage_migrations.py    #   数据迁移
│   ├── addon_params.py          #   运行时可观测配置
│   ├── utils.py                 #   通用工具
│   ├── utils_pipeline.py        #   管道专用工具 (36KB)
│   ├── constants.py             #   常量定义
│   ├── exceptions.py            #   异常类型
│   ├── _version.py              #   版本号
│   │
│   ├── kg/                      #   存储后端实现
│   │   ├── __init__.py          #     后端注册表 + 工厂
│   │   ├── factory.py           #     后端类解析
│   │   ├── shared_storage.py    #     跨进程共享状态 (锁/管道状态)
│   │   ├── networkx_impl.py     #     NetworkX 图 (默认)
│   │   ├── json_kv_impl.py      #     JSON KV 存储 (默认)
│   │   ├── json_doc_status_impl.py
│   │   ├── nano_vector_db_impl.py  #  NanoVectorDB (默认)
│   │   ├── neo4j_impl.py        #     Neo4j 图数据库
│   │   ├── postgres_impl.py     #     PostgreSQL + pgvector
│   │   ├── mongo_impl.py        #     MongoDB 统一存储
│   │   ├── milvus_impl.py       #     Milvus 向量数据库
│   │   ├── redis_impl.py        #     Redis KV + DocStatus
│   │   ├── qdrant_impl.py       #     Qdrant 向量数据库
│   │   ├── faiss_impl.py        #     FAISS 向量索引
│   │   ├── opensearch_impl.py   #     OpenSearch 统一存储
│   │   ├── memgraph_impl.py     #     Memgraph 图数据库
│   │   └── deprecated/          #     已弃用后端 (Chroma)
│   │
│   ├── llm/                     #   LLM 提供者绑定
│   │   ├── openai.py            #     OpenAI / Langfuse
│   │   ├── anthropic.py         #     Anthropic Claude
│   │   ├── gemini.py            #     Google Gemini
│   │   ├── ollama.py            #     Ollama 本地模型
│   │   ├── bedrock.py           #     AWS Bedrock
│   │   ├── hf.py                #     HuggingFace
│   │   ├── jina.py              #     Jina AI
│   │   ├── voyageai.py          #     Voyage AI
│   │   ├── zhipu.py             #     智谱 AI (GLM)
│   │   ├── lmdeploy.py          #     LMDeploy
│   │   ├── lollms.py            #     LOLLMS
│   │   ├── llama_index_impl.py  #     LlamaIndex 嵌入
│   │   ├── nvidia_openai.py     #     NVIDIA NIM
│   │   ├── azure_openai.py      #     Azure OpenAI
│   │   ├── binding_options.py   #     配置选项基类
│   │   ├── _vision_utils.py     #     视觉输入标准化
│   │   └── deprecated/          #     已弃用 (siliconcloud)
│   │
│   ├── parser/                  #   文档解析器层
│   │   ├── base.py              #     BaseParser 抽象
│   │   ├── registry.py          #     解析器注册表
│   │   ├── routing.py           #     路由引擎 (51KB)
│   │   ├── param_schema.py      #     参数语法解析
│   │   ├── debug.py             #     离线调试桩
│   │   ├── cli.py               #     调试 CLI
│   │   ├── noop.py              #     内部格式处理器
│   │   ├── native_dispatch.py   #     原生解析器分发
│   │   ├── plugins.py           #     第三方插件
│   │   ├── docx/                #     原生 DOCX 解析器
│   │   └── external/            #     外部解析器适配器
│   │       ├── _base.py         #       ExternalParserBase 模板方法
│   │       ├── _common.py       #       共享工具
│   │       ├── _manifest.py     #       Manifest IO
│   │       ├── _zip.py          #       ZIP 安全提取
│   │       ├── mineru/          #       MinerU API 适配器
│   │       └── docling/         #       Docling API 适配器
│   │
│   ├── chunker/                 #   文本分块策略
│   │   ├── token_size.py        #     固定 Token 分块 (F)
│   │   ├── recursive_character.py  #  递归字符分块 (R)
│   │   ├── semantic_vector.py   #     语义向量分块 (V)
│   │   └── paragraph_semantic.py #    段落语义分块 (P)
│   │
│   ├── sidecar/                 #   解析产物管理
│   │   ├── ir.py                #     IR 数据模型
│   │   ├── writer.py            #     产物写入器
│   │   └── backfill.py          #     回填逻辑
│   │
│   ├── api/                     #   FastAPI 服务器
│   │   ├── lightrag_server.py   #     主应用工厂 (~2500 行)
│   │   ├── run_with_gunicorn.py #     Gunicorn 启动器
│   │   ├── config.py            #     配置加载
│   │   ├── auth.py              #     认证处理器
│   │   ├── utils_api.py         #     API 工具
│   │   ├── routers/             #     路由模块
│   │   │   ├── document_routes.py  #   文档 CRUD
│   │   │   ├── query_routes.py     #   查询端点
│   │   │   ├── graph_routes.py     #   图谱端点
│   │   │   └── ollama_api.py       #   Ollama 兼容 API
│   │   ├── static/              #     Swagger 静态资源
│   │   └── webui/               #     构建后的前端产物
│   │
│   ├── evaluation/              #   评估工具
│   │   ├── eval_rag_quality.py  #     RAGAS 评估
│   │   └── offline_retrieval_check.py
│   │
│   ├── rerank.py                #   重排序
│   ├── tools/                   #   CLI 工具
│   │   ├── hash_password.py
│   │   ├── download_cache.py
│   │   ├── clean_llm_query_cache.py
│   │   └── rebuild_vdb.py
│   └── prompts/                 #   提示模板
│
├── lightrag_webui/              # 🟡 前端 React 应用
│   ├── src/
│   │   ├── main.tsx             #     入口
│   │   ├── App.tsx              #     App 外壳
│   │   ├── AppRouter.tsx        #     路由
│   │   ├── api/lightrag.ts      #     API 客户端 (40KB)
│   │   ├── features/            #     页面组件
│   │   │   ├── DocumentManager.tsx  #  文档管理 (67KB)
│   │   │   ├── RetrievalView.tsx    #  RAG 查询 (39KB)
│   │   │   ├── GraphViewer.tsx      #  图谱可视化 (13KB)
│   │   │   ├── LoginPage.tsx        #  登录
│   │   │   ├── ApiSite.tsx          #  Ollama API 测试
│   │   │   └── SiteHeader.tsx       #  站点头部
│   │   ├── components/          #     可复用组件
│   │   │   ├── documents/       #       文档对话框
│   │   │   ├── graph/           #       图谱控件
│   │   │   ├── retrieval/       #       聊天消息
│   │   │   ├── status/          #       状态卡片
│   │   │   └── ui/              #       UI 套件
│   │   ├── stores/              #     Zustand 状态管理
│   │   ├── hooks/               #     React Hooks
│   │   ├── lib/                 #     工具库
│   │   └── locales/             #     10 种语言翻译
│   ├── package.json
│   └── vite.config.ts
│
├── tests/                       # 🟢 测试套件 (镜像源码结构)
│   ├── api/                     #   API 测试
│   ├── chunker/                 #   分块策略测试
│   ├── parser/                  #   解析器测试
│   │   ├── external/mineru/
│   │   └── external/docling/
│   ├── pipeline/                #   管道测试
│   ├── kg/                      #   存储后端测试
│   │   ├── neo4j_impl/
│   │   ├── postgres_impl/
│   │   ├── milvus_impl/
│   │   ├── qdrant_impl/
│   │   ├── redis_impl/
│   │   ├── mongo_impl/
│   │   ├── opensearch_impl/
│   │   ├── memgraph_impl/
│   │   ├── faiss_impl/
│   │   ├── json_impl/
│   │   ├── nano_impl/
│   │   └── networkx_impl/
│   ├── llm/                     #   LLM 测试
│   ├── extraction/              #   提取测试
│   ├── sidecar/                 #   产物测试
│   ├── setup/                   #   安装向导测试
│   └── workspace/               #   工作区隔离测试
│
├── scripts/                     # 🔧 脚本
│   ├── test.sh                  #   测试运行器
│   └── setup/                   #   交互式环境向导
│       └── templates/           #     Docker Compose 模板
│
├── docs/                        # 📚 文档
├── examples/                    # 📖 示例代码
├── prompts/                     # 📝 提示模板文件
├── k8s-deploy/                  # ☸️  Kubernetes 部署清单
├── assets/                      # 🎨 静态资源
├── reproduce/                   # 🔬 复现脚本
│
├── pyproject.toml               # 项目元数据与依赖
├── setup.py                     # 向后兼容桩
├── Makefile                     # 构建/运行快捷方式
├── Dockerfile                   # 完整 Docker 镜像
├── Dockerfile.lite              # 轻量 Docker 镜像
├── Dockerfile.postgres          # PostgreSQL 专用镜像
├── docker-compose.yml           # 基础编排
├── docker-compose-full.yml      # 全功能编排
├── env.example                  # 环境变量模板 (56KB)
├── config.ini.example           # INI 配置模板
├── uv.lock                      # 依赖锁定文件
├── README.md                    # 英文 README
├── README-zh.md                 # 中文 README
├── README-ja.md                 # 日文 README
├── AGENTS.md                    # AI 代理指南
├── LICENSE                      # MIT 许可证
└── SECURITY.md                  # 安全策略
```

---

## 3. 核心架构

### 3.1 LightRAG 类组合 (Mixin 模式)

```python
# lightrag/lightrag.py
@final
@dataclass
class LightRAG(_RoleLLMMixin, _StorageMigrationMixin, _PipelineMixin):
    """MRO: LightRAG → _RoleLLMMixin → _StorageMigrationMixin → _PipelineMixin → object"""
```

`LightRAG` 由三个 Mixin 叠加组装而成：

| Mixin | 来源 | 职责 |
|---|---|---|
| `_RoleLLMMixin` | `llm_roles.py` | 角色化 LLM 路由、队列管理、热切换 |
| `_StorageMigrationMixin` | `storage_migrations.py` | 启动时数据迁移与修复 |
| `_PipelineMixin` | `pipeline.py` | 三层异步文档摄入管道 |

**关键设计**: `@final` 装饰器确保外部不可子类化；Mixin 分层是内部实现细节。公开 API (`ainsert`、`aquery`、`ainsert_custom_kg`、`initialize_storages` 等) 保持不变。

### 3.2 12 个存储实例

`LightRAG.__post_init__()` 中基于配置创建以下存储实例：

| 存储实例 | 类型 | 命名空间 ID | 用途 |
|---|---|---|---|
| `llm_response_cache` | `BaseKVStorage` | `llm_response_cache` | LLM 响应缓存 |
| `text_chunks` | `BaseKVStorage` | `text_chunks` | 文本分块存储 |
| `full_docs` | `BaseKVStorage` | `full_docs` | 完整文档内容 |
| `full_entities` | `BaseKVStorage` | `full_entities` | 实体元数据 |
| `full_relations` | `BaseKVStorage` | `full_relations` | 关系元数据 |
| `entity_chunks` | `BaseKVStorage` | `entity_chunks` | 实体→分块映射 |
| `relation_chunks` | `BaseKVStorage` | `relation_chunks` | 关系→分块映射 |
| `entities_vdb` | `BaseVectorStorage` | `entities_vdb` | 实体向量嵌入 |
| `relationships_vdb` | `BaseVectorStorage` | `relationships_vdb` | 关系向量嵌入 |
| `chunks_vdb` | `BaseVectorStorage` | `chunks_vdb` | 分块向量嵌入 |
| `chunk_entity_relation_graph` | `BaseGraphStorage` | `graph` | 知识图谱 |
| `doc_status` | `DocStatusStorage` | `doc_status` | 文档处理状态 |

### 3.3 关键数据流

```
┌──────────────────────────────────────────────────────────────────────┐
│                          摄 入 流 程                                  │
│                                                                      │
│  用户上传文档                                                          │
│      │                                                               │
│      ▼                                                               │
│  apipeline_enqueue_documents()                                       │
│      │  → 验证、去重、创建 docs_status 记录                            │
│      ▼                                                               │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐                        │
│  │ Parse    │───▶│ Analyze  │───▶│ Process  │                        │
│  │ Workers  │    │ Workers  │    │ Workers  │                        │
│  │ (Layer1) │    │ (Layer2) │    │ (Layer3) │                        │
│  └──────────┘    └──────────┘    └──────────┘                        │
│  原生/MinerU/     VLM 多模态      分块 → 提取 → 合并                    │
│  Docling 解析     图片/表格分析     → 存储后端                          │
└──────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────┐
│                          查 询 流 程                                  │
│                                                                      │
│  用户提问                                                              │
│      │                                                               │
│      ▼                                                               │
│  aquery() → aquery_llm()                                             │
│      │                                                               │
│      ├──▶ get_keywords_from_query()  (LLM 提取高低层关键词)            │
│      │                                                               │
│      └──▶ kg_query() 或 naive_query()                                │
│              │                                                       │
│              ├──▶ _get_node_data()     实体向量搜索                     │
│              ├──▶ _get_edge_data()     关系向量搜索                     │
│              ├──▶ 图谱遍历              找到关联实体/关系                 │
│              ├──▶ _get_vector_context() 分块向量检索                    │
│              ├──▶ 可选 rerank           重排序                         │
│              ├──▶ _apply_token_truncation() Token 预算限制              │
│              └──▶ LLM 生成回答          返回 QueryResult               │
└──────────────────────────────────────────────────────────────────────┘
```

### 3.4 主要公开方法

| 方法 | 类型 | 用途 |
|---|---|---|
| `initialize_storages()` | async | **必须调用** — 初始化所有存储后端 |
| `finalize_storages()` | async | 优雅关闭所有存储 |
| `ainsert(text, ids, file_paths)` | async | 摄入原始文本 |
| `ainsert_custom_chunks(chunks)` | async | 摄入预分块文本 |
| `aquery(query, param)` | async | RAG 查询 → LLM 生成回答 |
| `aquery_data(query, param)` | async | 仅检索（不调用 LLM），返回结构化数据 |
| `aquery_llm(query, param)` | async | 完整查询 → `QueryResult`（内容 + 原始数据） |
| `adelete_by_doc_id(doc_id)` | async | 删除文档及其图谱产物 |
| `adelete_by_entity(entity_name)` | async | 删除单个实体 |
| `adelete_by_relation(relation)` | async | 删除单个关系 |
| `aedit_entity/Relation(...)` | async | 编辑实体/关系属性 |
| `acreate_entity/Relation(...)` | async | 手动创建知识图谱节点/边 |
| `amerge_entities(entities)` | async | 合并重复实体 |
| `aget_docs_by_status(status)` | sync | 按处理状态查询文档 |
| `get_graph_labels()` | sync | 获取图谱标签分布 |
| `get_knowledge_graph(nodelabel, max_depth)` | sync | 导出知识图谱子图 |
| `aclear_cache()` | async | 清除 LLM 响应缓存 |
| `aexport_data(include_vector)` | async | 导出知识图谱到 CSV/Excel |
| `update_llm_role_config(role, config)` | sync | 运行时热切换角色 LLM 配置 |

---

## 4. 存储层

### 4.1 基类层次

```
StorageNameSpace (ABC, @dataclass)     # 所有存储的基类
│  - namespace, workspace, global_config
│  - initialize(), finalize(), drop()
│
├── BaseKVStorage (ABC)               # 键值存储基类
│   │  - get_by_id, get_by_ids, upsert, delete, filter_keys, drop
│   └── DocStatusStorage (ABC)        # 文档状态存储 (继承 KV)
│       - get_status_counts, get_docs_by_status, 
│         get_docs_paginated, get_doc_by_file_path
│
├── BaseVectorStorage (ABC)            # 向量存储基类
│   │  - embedding_func, cosine_better_than_threshold, meta_fields
│   │  - query, upsert, delete_entity, delete_entity_relation,
│   │    get_by_id, get_by_ids, delete
│   └── _validate_embedding_func() (静态检查器)
│
└── BaseGraphStorage (ABC)            # 图存储基类
    - has_node, has_edge, node_degree, edge_degree
    - get_node, get_edge, node_edges
    - upsert_node, upsert_edge, delete_node
    - remove_nodes, remove_edges
    - get_all_labels, get_knowledge_graph
    - get_popular_labels, search_labels
```

### 4.2 存储后端注册

**文件**: `lightrag/kg/__init__.py`

通过三级注册表管理所有后端：

| 注册表 | 用途 |
|---|---|
| `STORAGE_IMPLEMENTATIONS` | 类别 → 允许的实现列表 + 必需方法 |
| `STORAGE_ENV_REQUIREMENTS` | 实现 → 所需环境变量 |
| `STORAGES` | 实现名 → 模块路径 (懒加载) |

```python
STORAGE_IMPLEMENTATIONS = {
    "KV_STORAGE": {
        "JsonKVStorage", "RedisKVStorage", "PGKVStorage",
        "MongoKVStorage", "OpenSearchKVStorage"
    },
    "GRAPH_STORAGE": {
        "NetworkXStorage", "Neo4JStorage", "PGGraphStorage",
        "MongoGraphStorage", "MemgraphStorage", "OpenSearchGraphStorage"
    },
    "VECTOR_STORAGE": {
        "NanoVectorDBStorage", "MilvusVectorDBStorage", "PGVectorStorage",
        "FaissVectorDBStorage", "QdrantVectorDBStorage",
        "MongoVectorDBStorage", "OpenSearchVectorDBStorage"
    },
    "DOC_STATUS_STORAGE": {
        "JsonDocStatusStorage", "RedisDocStatusStorage",
        "PGDocStatusStorage", "MongoDocStatusStorage", "OpenSearchDocStatusStorage"
    }
}
```

### 4.3 后端特性矩阵

| 后端 | KV | 向量 | 图 | DocStatus | 默认 | 适用场景 |
|---|---|---|---|---|---|---|
| **JSON** | ✅ | — | — | ✅ | ✅ KV | 本地开发 |
| **NanoVectorDB** | — | ✅ | — | — | ✅ 向量 | 本地开发 |
| **NetworkX** | — | — | ✅ | — | ✅ 图 | 本地开发 |
| **PostgreSQL** | ✅ | ✅ | ✅ | ✅ | — | 生产环境 (单一数据库) |
| **MongoDB** | ✅ | ✅ | ✅ | ✅ | — | 生产环境 (文档数据库) |
| **OpenSearch** | ✅ | ✅ | ✅ | ✅ | — | 生产环境 (搜索引擎) |
| **Neo4j** | — | — | ✅ | — | — | 图数据库专用 |
| **Milvus** | — | ✅ | — | — | — | 大规模向量检索 |
| **Qdrant** | — | ✅ | — | — | — | 高性能向量检索 |
| **Redis** | ✅ | — | — | ✅ | — | 高速缓存 |
| **FAISS** | — | ✅ | — | — | — | 离线向量索引 |
| **Memgraph** | — | — | ✅ | — | — | 实时图分析 |

> **工作区隔离**: 每个 `LightRAG` 实例可通过 `workspace` 参数实现数据隔离。不同后端实现方式不同：文件后端使用子目录，集合后端使用前缀，关系数据库使用列过滤。

---

## 5. 文档摄入管道

### 5.1 三层异步工作管道 (`lightrag/pipeline.py`)

```
apipeline_enqueue_documents()   ← 公共入口: 验证、去重、创建状态、入队
         │
         ▼
┌─────────────────────────┐
│  Layer 1: Parse Workers │  解析层 — 每个 ParserSpec.queue_group 一个
│  ┌─────────────────────┐│
│  │ Native | MinerU     ││  从文件中提取结构化内容
│  │ Docling | Legacy    ││  → 产物写入 sidecar (IR JSON + blocks.jsonl)
│  └─────────────────────┘│  → q_analyze
├─────────────────────────┤
│  Layer 2: Analyze       │  分析层 — VLM 多模态处理
│  Workers                │
│  ┌─────────────────────┐│  图片/表格/公式的多模态分析
│  │ VLM 视觉语言模型     ││  → 增强实体提取结果
│  └─────────────────────┘│  → q_process
├─────────────────────────┤
│  Layer 3: Process       │  处理层
│  Workers                │
│  ┌─────────────────────┐│
│  │ Chunking (F/R/V/P)  ││  选择分块策略 → 切分文本
│  │ Entity Extraction   ││  LLM 提取实体和关系
│  │ Merge to KG         ││  合并入知识图谱 + 向量库
│  └─────────────────────┘│
└─────────────────────────┘
```

### 5.2 管道并发契约

管道通过 `pipeline_status`（位于 `lightrag.kg.shared_storage` 中的每个工作区共享字典）协调并发写入。在 `get_namespace_lock("pipeline_status")` 下原子地检查/设置以下字段：

| 字段 | 含义 | 阻塞谁 |
|---|---|---|
| `busy` | 管道忙碌中 | — (单独不阻塞入队) |
| `destructive_busy` | 正在执行破坏性操作 (清库/删文档) | 阻塞入队 |
| `scanning` | 正在扫描 | 阻塞重叠扫描 |
| `scanning_exclusive` | 扫描的分类阶段 | 阻塞入队 |
| `pending_enqueues` | 途中的上传计数 | 阻塞扫描启动 |
| `request_pending` | 请求处理循环重新查询 | — (信号旗) |

**文档生命周期**: `PENDING → PARSING → ANALYZING → PROCESSING → PROCESSED | FAILED`

---

## 6. 解析器层

### 6.1 设计模式: 注册表 → 路由 → 懒加载

```
┌────────────────────────────────────────────────────┐
│               registry.py                           │
│  _REGISTRY: dict[str, ParserSpec]                  │
│  ┌──────────────────────────────────────────────┐  │
│  │ ParserSpec(name="native", suffixes=[".docx"],│  │
│  │   impl="lightrag.parser.native_dispatch:     │  │
│  │         NativeParser", endpoint=None, ...)   │  │
│  │ ParserSpec(name="mineru", suffixes=[".pdf"], │  │
│  │   impl="lightrag.parser.external.mineru:     │  │
│  │         MinerUParser", endpoint="MINERU_URI",│  │
│  │   endpoint_optional=False)                   │  │
│  │ ParserSpec(name="docling", suffixes=[".pdf"],│  │
│  │   impl="...docling:DoclingParser", ...)      │  │
│  └──────────────────────────────────────────────┘  │
│  • register_parser(spec)  — 第三方插件注册入口      │
│  • get_parser(name)       — 懒导入实现类            │
└──────────────────────┬─────────────────────────────┘
                       │
┌──────────────────────▼─────────────────────────────┐
│               routing.py (51KB)                     │
│  resolve_parser_directives()                       │
│  resolve_file_parser_engine()                      │
│  优先级级联:                                        │
│  1. 文件名提示 (.[engine] 后缀)                    │
│     e.g. report.[mineru].pdf                       │
│  2. LIGHTRAG_PARSER 环境变量 (glob → engine)       │
│  3. 后缀默认映射                                    │
│                                                      │
│  parse_process_options()                           │
│  resolve_chunk_options()                           │
│  parser_rules_from_env()                           │
└────────────────────────────────────────────────────┘
```

### 6.2 BaseParser 协议

```python
class BaseParser(ABC):
    engine_name: str

    @abstractmethod
    async def parse(self, ctx: ParseContext) -> ParseResult:
        """输入: ParseContext (rag实例, doc_id, 文件路径)
           输出: ParseResult (content_list, modality_list, source_uri, ...)"""
```

### 6.3 外部解析器适配器模式

`mineru` 和 `docling` 适配器共享相同架构：

```
ExternalParserBase (Template Method)
  │
  ├── is_bundle_valid()    ← 子类实现: 检查缓存是否有效
  ├── download_into()      ← 子类实现: HTTP 上传→轮询→下载
  └── build_ir()           ← 子类实现: 原始输出 → LightRAG IRDoc

每个适配器包含:
├── parser.py   → 适配器主类
├── client.py   → HTTP API 编排
├── cache.py    → 磁盘缓存, 缓存命中检测, 强制重新解析
├── ir_builder.py → 规范化转换
└── manifest.py → Manifest 构建
```

### 6.4 文件存储位置详解

**环境变量控制**:

| 变量 | 默认值 | 说明 |
|---|---|---|
| `INPUT_DIR` | `./inputs` | 上传文件原始存放目录 |
| `WORKING_DIR` | `./rag_storage` | 知识图谱/向量嵌入等运行时数据 |

**完整解析存储路径**:

```
INPUT_DIR (默认 ./inputs)
│
├── 【上传文件暂存】
│   ├── report.pdf                             ← 用户上传的原始文件
│   └── guide.docx                             ← 解析完成后可能移走归档
│
└── __parsed__/                                ← 解析产物根目录
    │
    └── <文件名>.parsed/                       ← 每个文档一个独立目录
        ├── <文件名>.blocks.jsonl              ← 解析出的文本块（JSONL 格式）
        │                                        每行 = { "content": "段落文本", ... }
        │
        ├── <文件名>.blocks.assets/            ← 🔴 拆分出的图片/表格/公式
        │   ├── image_001.png                  ←     提取的图片
        │   ├── image_002.jpg                  ←     提取的图片
        │   └── table_001.png                  ←     提取的表格截图
        │
        ├── document.ir.json                   ← 结构化 IR 文档（中间表示）
        │
        └── <文件名>.pdf                       ← 解析完成后原始文件移入归档
            (Docling 解析完成后自动移入)
```

**各解析引擎的原始缓存目录**:

```
./inputs/__parsed__/
├── <文件>.docling_raw/    ← Docling 引擎的 HTTP 下载原始包（含 JSON + 图片）
│   ├── <stem>.json        ←     Docling-serve 返回的主 JSON
│   └── ...                ←     图片等附件
│
└── <文件>.mineru_raw/     ← MinerU 引擎的 HTTP 下载原始包（同上结构）
```

**生命周期**:

```
上传 → 暂存到 INPUT_DIR → 解析引擎处理 → 产物写入 __parsed__/
                                              │
                    有文字内容: blocks.jsonl 有内容 → 分块 → 嵌入 → 图谱
                    无文字/纯图片: blocks.jsonl 为空 → 自动跳过 ✅
                    (validate_ir 修改后不再报错)
                                              │
                    原始文件: 从 INPUT_DIR 移入 __parsed__/ 归档
                    缓存:     docling_raw/mineru_raw 保留供下次命中
```

**与远程存储的关系**:

```
┌──────────────────┬────────────────────┬─────────────────────┐
│ 数据类型           │ 存储位置             │ 能否用 MinIO/S3      │
├──────────────────┼────────────────────┼─────────────────────┤
│ 上传的原始文件      │ 本地 INPUT_DIR      │ ❌ 不支持 (future)   │
│ 解析产物 (IR JSON) │ 本地 __parsed__/    │ ❌ 不支持 (future)   │
│ 拆分的图片/表格     │ .blocks.assets/     │ ❌ 不支持            │
│ 知识图谱 (实体关系) │ Neo4j 等远程 DB     │ N/A (图数据库)       │
│ 向量嵌入           │ Qdrant 等远程 DB    │ N/A (向量数据库)     │
│ LLM 响应缓存       │ Redis 等远程 DB     │ N/A (KV 存储)        │
│ 文档状态           │ Redis 等远程 DB     │ N/A (DocStatus)      │
└──────────────────┴────────────────────┴─────────────────────┘
```

> **注意**: `__parsed__/` 目录会随着上传文件增多而变大。对于 1500 个 PDF (280MB)，预计解析产物总大小约 **500-800MB**（含图片 + JSON + 原始缓存）。

---

## 7. 分块策略

**文件**: `lightrag/chunker/`

两种契约共存：
- **旧契约**: `chunking_by_token_size(tokenizer, content, split_by_character, split_by_character_only, chunk_overlap_token_size, chunk_token_size)` — 6 个位置参数
- **新契约**: `(tokenizer, content, chunk_token_size, *, <策略特定参数>)` — 用于 process_options 显式选择

| 文件 | 策略 ID | 策略名称 | 算法描述 |
|---|---|---|---|
| `token_size.py` | `"F"` | 固定 Token | 固定大小滑动窗口 + 重叠。默认策略。 |
| `recursive_character.py` | `"R"` | 递归字符 | 包装 LangChain RecursiveCharacterTextSplitter。在分隔符级联 (`\n\n` → `\n` → ` ` → ``) 上递归分割。 |
| `semantic_vector.py` | `"V"` | 语义向量 | 包装 LangChain SemanticChunker。句子级嵌入相似度找断点。需要 EmbeddingFunc。R 策略为回退。 |
| `paragraph_semantic.py` | `"P"` | 段落语义 | **标题感知语义分块器**。消费 `.blocks.jsonl` 产物。管道: HeadingBlocks → TableRowSplit → AnchorSplit → HeadingGlue → LevelMerge。 |

> **分块策略选择**: `process_options` 字符串决定每个文档的策略，如 `"F"` (固定), `"R(chunk_token_size=800)"` (递归+参数)。分块选项快照在入队时从 `addon_params["chunker"]` 捕获并持久化。

---

## 8. LLM 提供者层

### 8.1 提供者绑定

| 文件 | 关键函数 | 提供者 |
|---|---|---|
| `openai.py` (52KB) | `openai_complete_if_cache()`, `openai_embed()` | OpenAI / Langfuse 观测 |
| `anthropic.py` | `anthropic_complete_if_cache()` | Anthropic Claude |
| `gemini.py` | `gemini_complete_if_cache()`, `gemini_embed()` | Google Gemini |
| `ollama.py` | `ollama_complete_if_cache()`, `ollama_embed()` | Ollama (本地/云端) |
| `bedrock.py` | `bedrock_complete_if_cache()`, `bedrock_embed()` | AWS Bedrock |
| `hf.py` | `hf_embed()` | HuggingFace 推理 API |
| `jina.py` | `jina_embed()` | Jina AI 嵌入 |
| `voyageai.py` | `voyageai_embed()` | Voyage AI 嵌入 |
| `zhipu.py` | `zhipu_complete_if_cache()` | 智谱 AI (GLM) |
| `lmdeploy.py` | `lmdeploy_complete_if_cache()` | LMDeploy (Turbomind) |
| `lollms.py` | `lollms_complete_if_cache()` | LOLLMS |
| `llama_index_impl.py` | `llama_index_embed()` | LlamaIndex 嵌入 |
| `nvidia_openai.py` | `nvidia_openai_embed()` | NVIDIA NIM |
| `azure_openai.py` | `azure_openai_complete_if_cache()` | Azure OpenAI |

### 8.2 公共基础设施

- **`binding_options.py`**: `BindingOptions` 基类 — 标准化各提供者的配置管理。自动生成环境变量名、CLI 参数、`.env` 样本。
- **`_vision_utils.py`**: 共享图像输入标准化 — 接受 base64 字符串、数据 URL 或带有 `base64`/`mime_type`/`source_id` 键的字典。
- **重试模式**: 所有 `*_complete_if_cache` 使用 `@retry(stop=stop_after_attempt(3), wait=wait_exponential(...))`。

---

## 9. 查询系统

### 9.1 QueryParam 数据类

```python
@dataclass
class QueryParam:
    mode: str = "mix"              # local | global | hybrid | mix | naive | bypass
    top_k: int = 60                # KG 实体/关系检索数量
    chunk_top_k: int = 20          # 文本分块检索数量
    max_entity_tokens: int = 6000  # 实体 token 预算
    max_relation_tokens: int = 8000 # 关系 token 预算
    max_total_tokens: int = 30000  # 总 token 预算
    enable_rerank: bool = True     # 是否启用重排序
    stream: bool = False           # 流式输出
    user_prompt: str = ""          # 用户附加指令
    conversation_history: list = None  # 对话历史
    response_type: str = ""        # 响应格式
    hl_keywords: list = None       # 高层关键词 (优先级高)
    ll_keywords: list = None       # 低层关键词
```

### 9.2 五种检索模式

```
┌──────────┬──────────────────────────────────────────────────────────┐
│ 模式      │ 行为                                                      │
├──────────┼──────────────────────────────────────────────────────────┤
│ local    │ 实体中心: 向量搜索实体 → 图谱扩展找关系 → 检索关联分块      │
│ global   │ 关系中心: 向量搜索关系 → 图谱扩展找实体 → 检索关联分块      │
│ hybrid   │ 轮询交错: local + global 结果交替混合                       │
│ mix      │ KG + 向量: KG 结果 + 额外 naive 分块向量搜索 (推荐配合rerank)│
│ naive    │ 纯向量: 仅分块向量相似度搜索，不使用图谱                      │
│ bypass   │ 直通: LLM 直接回答，不做任何检索                            │
└──────────┴──────────────────────────────────────────────────────────┘
```

### 9.3 核心查询流程 (operate.py ~5000 行)

| 函数 | 用途 |
|---|---|
| `extract_entities()` | 主要实体提取 — 分块文本 → LLM 提取每个分块的实体+关系。支持 JSON 模式或传统文本模式。包含 gleaning 循环。 |
| `merge_nodes_and_edges()` | 两阶段合并 — Phase 1: 并发处理所有实体 (去重、合并描述、upcert 到图谱+VDB)。Phase 2: 处理所有关系。 |
| `kg_query()` | 图谱感知查询 — 提取关键词 → `_build_query_context()` → 选择模式 → 检索实体/关系/分块 → token 截断 → LLM 生成。 |
| `naive_query()` | 纯向量搜索 — 分块 VDB 相似度搜索 → rerank → token 截断。 |
| `get_keywords_from_query()` | LLM 调用提取高低层关键词。 |
| `_build_query_context()` | 中央查询编排 — 分派到 `_get_node_data()`, `_get_edge_data()`, 向量搜索, 合并, 截断。 |
| `_apply_token_truncation()` | 统一的 token 预算执行 — 跨实体/关系/分块。 |
| `_find_most_related_edges_from_entities()` | 给定顶级实体，从图谱找关联关系。 |
| `_find_related_text_unit_from_entities()` | 实体反向映射到源文本分块。 |

---

## 10. 角色化 LLM 配置

### 10.1 角色注册表

```python
ROLES = (
    RoleSpec("extract",  "EXTRACT",  "实体提取 LLM 函数"),    # 实体提取阶段
    RoleSpec("keyword",  "KEYWORD",  "关键词 LLM 函数"),      # 关键词提取阶段
    RoleSpec("query",    "QUERY",    "查询 LLM 函数"),        # RAG 查询回答阶段
    RoleSpec("vlm",      "VLM",      "VLM 函数"),             # 多模态/视觉分析阶段
)
```

### 10.2 设计意图

每个角色拥有独立的 LLM 配置 + 独立的并发队列:

```
角色 "extract" ──▶ priority_limit_async_func_call ──▶ LLM API
                      (独立 max_async, timeout)
角色 "keyword" ──▶ priority_limit_async_func_call ──▶ LLM API
角色 "query"   ──▶ priority_limit_async_func_call ──▶ LLM API
角色 "vlm"     ──▶ priority_limit_async_func_call ──▶ VLM API
```

**关键**: 基础 `llm_model_func` **不** 被队列包装 — 所有代码路径使用角色包装器，并发控制在角色级别执行。

### 10.3 运行时热切换

```python
# 运行时替换 extract 角色的 LLM
rag.update_llm_role_config("extract", RoleLLMConfig(
    func=custom_extract_llm,
    max_async=8,
    timeout=120.0
))
```

---

## 11. API 服务器

### 11.1 架构 (`lightrag/api/lightrag_server.py`)

```
create_app(args) → FastAPI
  │
  ├── 1. 验证 LLM/嵌入绑定 (14 种提供者)
  ├── 2. 创建 LLMConfigCache 集中配置缓存
  ├── 3. 初始化 DocumentManager 文件监视
  ├── 4. 构建 LightRAG 实例 (含角色 LLM 配置)
  ├── 5. 注册 role_llm_builder (运行时热切换)
  │
  └── 6. 挂载路由
       ├── create_document_routes()  → 文档 CRUD
       ├── create_query_routes()     → 查询端点 + 流式
       ├── create_graph_routes()     → 图谱探索
       └── OllamaAPI                 → /api (Ollama 兼容)
```

### 11.2 API 端点一览

| 方法 | 路径 | 用途 |
|---|---|---|
| `GET` | `/health` | 健康检查 |
| `GET/POST` | `/auth-status`, `/login` | 认证 |
| `POST` | `/documents/text` | 文本插入 |
| `POST` | `/documents/upload` | 文件上传 |
| `GET` | `/documents` | 文档列表 |
| `DELETE` | `/documents/delete_document` | 删除文档 |
| `GET/POST` | `/documents/paginated` | 分页文档列表 |
| `POST` | `/documents/scan` | 扫描输入目录 |
| `GET` | `/documents/scan-progress` | 扫描进度 |
| `POST` | `/documents/reprocess_failed` | 重新处理失败文档 |
| `GET` | `/documents/status_counts` | 状态统计 |
| `GET/POST` | `/query` | 查询 (文本) |
| `GET/POST` | `/query/stream` | 查询 (流式) |
| `GET/POST` | `/query/data` | 查询 (仅数据) |
| `GET` | `/graph/labels` | 图谱标签 |
| `POST` | `/graph/search` | 图谱搜索 |
| `POST` | `/api/generate` | Ollama 兼容生成 |
| `POST` | `/api/chat` | Ollama 兼容对话 |

### 11.3 CLI 入口点

| 命令 | 用途 |
|---|---|
| `lightrag-server` | 生产服务器 (uvicorn) |
| `lightrag-gunicorn` | 多 Worker (Gunicorn) |
| `lightrag-hash-password` | 密码哈希工具 |
| `lightrag-download-cache` | 下载缓存 |
| `lightrag-clean-llmqc` | 清理 LLM 查询缓存 |
| `lightrag-rebuild-vdb` | 重建向量数据库 |
| `python -m lightrag.parser.cli` | 解析器调试 CLI |

---

## 12. 前端 WebUI

### 12.1 技术栈

```
React 19 · TypeScript 6.0 · Vite 8 · Tailwind CSS 4 · Zustand 5
Sigma.js 3.0 · graphology · react-markdown · mermaid · KaTeX
react-dropzone · cmdk · Radix UI · i18next
```

### 12.2 核心架构

| 层 | 关键文件 | 职责 |
|---|---|---|
| **入口** | `main.tsx`, `App.tsx`, `AppRouter.tsx` | App 外壳 + React Router 路由 |
| **API** | `api/lightrag.ts` (40KB) | 单一 API 客户端 — 所有后端 HTTP 调用 |
| **页面** | `features/DocumentManager.tsx` (67KB) | 文档上传/管理 |
| | `features/RetrievalView.tsx` (39KB) | RAG 查询/聊天 |
| | `features/GraphViewer.tsx` (13KB) | 知识图谱可视化 |
| | `features/LoginPage.tsx` | 登录 |
| | `features/ApiSite.tsx` | Ollama API 测试 |
| **状态** | `stores/state.ts` (13KB) | 全局状态 |
| | `stores/settings.ts` (11KB) | 用户设置 |
| | `stores/graph.ts` (18KB) | 图谱数据 |
| **Hook** | `hooks/useLightragGraph.tsx` (49KB) | 核心图谱交互 — 桥接 Sigma.js 和 React |
| **i18n** | `locales/` (10 种语言) | en, zh, ja, ko, ar, de, fr, ru, uk, vi |

---

## 13. 配置与部署

### 13.1 配置方式

1. **环境变量** (`.env` 文件): 主要方式。`env.example` (56KB) 包含所有可配置变量的完整参考。
2. **构造函数参数**: `LightRAG(working_dir="...", kv_storage="PGKVStorage", ...)`
3. **`config.ini.example`**: INI 格式配置模板

### 13.2 Docker 部署

| 文件 | 用途 |
|---|---|
| `Dockerfile` | 完整镜像 (API + 离线 LLM + 离线存储) |
| `Dockerfile.lite` | 轻量镜像 (仅 API) |
| `Dockerfile.postgres` | PostgreSQL 专用镜像 |
| `docker-compose.yml` | 基础编排 |
| `docker-compose-full.yml` | 全功能编排 (多服务) |
| `docker-compose.podman.yml` | Podman 兼容编排 |
| `docker-entrypoint.sh` | 容器入口脚本 |

### 13.3 Kubernetes

```
k8s-deploy/
└── databases/   # 数据库部署清单
```

### 13.4 Makefile 工作流

```bash
make dev              # 1. 引导: uv sync + bun install + 构建前端
make env-base         # 2. 配置 LLM/嵌入/重排序
make env-storage      # 3. 配置存储后端 (可选)
make env-server       # 4. 配置服务器/安全/SSL (可选)
make env-validate     # 验证 .env
make env-security-check  # 安全审计
make env-backup       # 备份 .env
```

---

## 14. 开发命令

### 14.1 安装

```bash
# 使用 uv 安装
uv sync
source .venv/bin/activate

# 安装 API 支持
uv sync --extra api

# 安装特定可选依赖
uv sync --extra offline-storage   # 存储后端
uv sync --extra offline-llm       # LLM 提供者
uv sync --extra test              # 测试
uv sync --extra evaluation        # 评估
uv sync --extra observability     # 可观测性
```

### 14.2 启动服务器

```bash
# 生产
lightrag-server

# 开发
uvicorn lightrag.api.lightrag_server:app --reload

# 多 Worker
lightrag-gunicorn
```

### 14.3 前端

```bash
cd lightrag_webui
bun install --frozen-lockfile
bun run dev          # 开发服务器
bun run build        # 生产构建
bun test             # 运行测试
bun run lint         # ESLint
```

### 14.4 代码检查

```bash
ruff check .
```

---

## 15. 测试体系

### 15.1 运行测试

```bash
# 通过脚本运行
./scripts/test.sh tests

# 运行特定文件
./scripts/test.sh tests/kg/test_graph_storage.py

# 自定义 Workers
./scripts/test.sh tests --test-workers 4

# 直接 pytest
pytest tests/ -xvs
```

### 15.2 测试组织

```
tests/
├── api/             # API 层: 认证、CORS、路径前缀、OllamaChat、
│                    #   文档路由、图谱路由验证
├── chunker/         # 4 种分块策略 + chunk_options 持久化
├── parser/          # 原生 DOCX (字节等价黄金夹具)、Markdown、CLI、
│   ├── external/    #   插件、注册表、引擎参数
│   ├── mineru/      # MinerU: 缓存/客户端/IR构建器/产物测试
│   └── docling/     # Docling: 同上
├── pipeline/        # 内容哈希、doc_status、多模态、管道取消
├── kg/              # 每个后端一个子目录 + 跨后端测试
│   ├── neo4j_impl/ postgres_impl/ milvus_impl/
│   ├── qdrant_impl/ redis_impl/ mongo_impl/
│   ├── opensearch_impl/ memgraph_impl/ faiss_impl/
│   ├── json_impl/ nano_impl/ networkx_impl/
│   └── (batch_graph_operations, file_atomic, concurrency_slots)
├── llm/             # 每个提供者的行为测试 + 跨提供者: 嵌入、并发、VLM、角色
├── extraction/      # 实体提取稳定性、关键词解析
├── sidecar/         # 产物写入、回填、URI 解析
├── setup/           # 安装向导测试
└── workspace/       # 工作区隔离、迁移隔离
```

**标记** (`pytest.ini`): `offline`, `integration`, `requires_db`, `requires_api`  
**默认跳过**: integration 测试 (`-m "not integration"`)

---

## 16. 关键设计模式

### 16.1 Mixin 组合模式

```python
LightRAG → _RoleLLMMixin → _StorageMigrationMixin → _PipelineMixin → object
```

通过多继承将横切关注点模块化，保持主类 `@final` 防止外部子类化。

### 16.2 模板方法模式 (解析器)

`ExternalParserBase.parse()` 实现完整流程，子类只需实现三个钩子：`is_bundle_valid()`, `download_into()`, `build_ir()`。

### 16.3 注册表 + 懒加载 (解析器/存储)

- **解析器**: `_REGISTRY` 存储轻量 `ParserSpec`，`get_parser()` 通过 `importlib` 懒加载实现
- **存储**: `STORAGES` 字典映射名称→模块路径，工厂函数按需加载

### 16.4 角色基队列模式

每个 LLM 角色通过 `priority_limit_async_func_call` 获得独立队列，实现并发隔离和优先级控制。

### 16.5 同步/异步双面 API

每个关键操作 (`insert`, `query`, `delete_by_entity` 等) 同时提供 `sync` 和 `async` (`ainsert`, `aquery`, `adelete_by_entity`) 变体。

### 16.6 可观测配置

`ObservableAddonParams` 字典子类 — 变更时触发回调，使运行时配置变更即时生效。

### 16.7 图片嵌入管线（CLIP + Qdrant）

**新增于 2026-06-30**，实现文档解析时自动提取并嵌入图片：

**流程**:
```
Docling 解析 PDF → .blocks.assets/ (图片) + .drawings.json (元数据)
    → CLIP ViT-B/32 生成 512 维视觉嵌入
    → 存入 Qdrant: lightrag_vdb_images 集合
    → 搜索时并行查询文本 + 图片 → 结果中返回 image_path
    → 清空/删除时同步清理图片数据
```

**修改文件**:

| 文件 | 改动 |
|---|---|
| `lightrag/namespace.py` | 新增 `VECTOR_STORE_IMAGES = "images"` |
| `lightrag/lightrag.py` | images_vdb 存储实例、初始化/最终化/刷新/清理 |
| `lightrag/pipeline.py` | `_embed_document_images()` 方法，解析后自动调用 |
| `lightrag/operate.py` | `_perform_kg_search` 中并行查询 images_vdb |
| `lightrag/embeddings/image_embedder.py` | CLIP 模型懒加载、批量嵌入 |
| `lightrag/kg/postgres_impl.py` | PG 表映射 `LIGHTRAG_VDB_IMAGES` |
| `lightrag/api/routers/document_routes.py` | clear_documents 包含 images_vdb |

**关键函数**:
- `embed_images(paths)` → 异步生成 CLIP 512 维嵌入
- `_embed_document_images()` → 读取 `.drawings.json` + `.blocks.assets/` → 嵌入 → 存入 images_vdb
- 搜索时: `images_vdb.query()` 并行于 `chunks_vdb.query()`，图片作为 `source_type: "image"` 的 chunk 返回

**CLIP 模型**: `openai/clip-vit-base-patch32`，可通过 `IMAGE_EMBEDDING_MODEL` 环境变量配置

**MinIO 图片存储** (2026-06-30 新增):

| 变量 | 默认值 | 说明 |
|---|---|---|
| `MINIO_ENDPOINT` | `106.15.102.66:9000` | MinIO 服务地址 |
| `MINIO_ACCESS_KEY` | `minioadmin` | 访问密钥 |
| `MINIO_SECRET_KEY` | `minioadmin` | 秘密密钥 |
| `MINIO_BUCKET` | `lightrag-images` | 存储桶名称 |
| `MINIO_SECURE` | `false` | 是否使用 HTTPS |

- 图片上传到 MinIO 后，`images_vdb` 的 `image_path` 存储 MinIO HTTP URL（如 `http://106.15.102.66:9000/lightrag-images/...`）
- MinIO 不可用时自动降级为本地文件路径，通过 `/sidecar/image` 端点提供
- `clear_documents` 清空时自动删除 MinIO 中所有图片

---

## 🎯 附录：快速上手示例

```python
import asyncio
from lightrag import LightRAG, QueryParam
from lightrag.llm.openai import gpt_4o_mini_complete, openai_embed
from lightrag.utils import EmbeddingFunc

async def main():
    # 1. 创建实例
    rag = LightRAG(
        working_dir="./rag_storage",
        llm_model_func=gpt_4o_mini_complete,
        embedding_func=EmbeddingFunc(
            embedding_dim=1536,
            max_token_size=8192,
            func=openai_embed.func  # 使用 .func 访问原始函数
        ),
        top_k=60,
        chunk_top_k=20,
    )

    # 2. ⚠️ 必须初始化存储
    await rag.initialize_storages()

    # 3. 摄入文档
    await rag.ainsert("Python 是一种高级编程语言...")

    # 4. 查询
    result = await rag.aquery(
        "什么是 Python？",
        param=QueryParam(mode="hybrid")
    )
    print(result)

    # 5. 清理
    await rag.finalize_storages()

asyncio.run(main())
```

---

> **本文档基于代码知识图谱自动生成**  
> 代码索引: 8651 节点 · 42486 边 · 489+ 类 · 16 个边类型  
> 边类型: CALLS (11679) · USAGE (10418) · DEFINES (7921) · TESTS (2913) · 等  
> 架构簇: 12 个 Leiden 社区检测簇，包含 tests、lightrag、lightrag_webui 等
